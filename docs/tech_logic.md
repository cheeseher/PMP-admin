# 逻辑规范

## 1. 概览
暂无详细规范。请在此维护业务规则和技术实现，防止破坏现有逻辑。

## 2. 核心逻辑记录
- 接口定义
- 数据库结构
- 状态管理(Store)
- 核心业务链路图
- 算法实现
- **新增指令逻辑**：在机器人预设指令中新增了"USDT汇率"（序号20，`id: 120`），格式要求支持多种方式 `Q 或 q（全部），Z 或 z（支付宝），V 或 v（微信），K 或 k（银行卡）`。前端页面 `CommandManagement.vue` 已预定义此内容。
